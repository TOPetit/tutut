name: Test

on:
  release:
    types: created
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  # Data generation job
  Generation:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hi
        run: echo ${{GITHUB_REF}}
